{
  "name": "home-pruner",
  "version": "0.1.0",
  "description": "An interactive CLI tool to delete local git branches safely.",
  "keywords": [],
  "author": "user6m",
  "license": "MIT",
  "type": "module",
  "bin": {
    "home-pruner": "dist/main.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/cli.js --banner:js=\"#!/usr/bin/env node\"",
    "dev": "npm run build -- --sourcemap=inline",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
